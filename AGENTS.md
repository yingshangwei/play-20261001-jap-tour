# 仓库协作规则

本文件只保留执行规则。配置或界面改动前先读 [配置化架构](docs/development/guide-platform-architecture.md)，其他文档见 [文档地图](MAP.md)。

## 开工前确认范围

- 先看相关需求和 `git status`，保护已有修改。区分本次是改攻略内容、共享界面，还是框架能力。
- 改内容前说明目标配置、`guideId` 和日期；已明确就沿用，不明确就先问。不能把当前浏览器标签或默认首页当成用户选择。
- 改共享界面前说明受影响的组件、模板和配置；不要只修正在预览的那一套。

## 内容与界面分开改

- 行程、时间、点位、交通、餐厅、文案和图片说明改目标配置及其资源；统一布局、样式和交互改共享组件或模板。不要求所有样式都放进全局样式文件。
- 通用组件不导入具体攻略包，不按攻略标识、日期、地名或路由写特例。注册表和路由只负责加载配置、装配页面和生成页面元信息。
- 配置可选择已注册模板和有类型约束的展示选项，不能塞组件代码、任意标记或样式凑界面。需要新能力时，扩展通用模型、渲染器和测试；保留已有模板的合理差异。

## 防止配置串改

- 配置 2 继承配置 1 的部分数据。改共享源前检查引用；只改一个版本时，保证另一版本最终内容不变。
- 对共享对象和数组使用不可变更新，嵌套修改也复制对应层级；新增版本复用稳定数据，只覆盖必要差异。
- 点位、每日顺序和交通各有一个权威来源，地图、动画及可派生手账从中生成；同步核对摘要、餐厅日期、照片和下载文件。
- 切换配置后，详情、返回链接、页面元信息、地图筛选和播放状态必须属于当前版本；开发示例不能混入关西专属内容。

## 修改关西行程前必读

必须先读 [关西旅行约束](docs/requirements/kansai-2026/constraints.md)。固定日期、必保留项目、作息和行李安排不能自行改变；具体时间以选中配置为准，保留用户已确认的版本差异。这些旅行约束不适用于其他旅行或开发示例。

## 旅行研究工具与参考优先级

后续涉及行程复核、交通、景点、住宿或餐厅筛选时，必须主动使用对应的已就绪工具和 skill，不必等用户再次提醒。按当前问题选择必要工具，不要求每次把全部工具跑一遍；纯界面或文档排版改动不触发外部查询。开始前读 [旅行工具状态与调用入口](docs/research/travel-tooling-2026-09.md)，区分已安装、已实测、需要刷新授权和验证失败。

先保留用户已确认的约束，并读取相关仓库材料；涉及用户提供的飞书文档时优先尝试现有 `lark-cli` 的只读访问，认证或文档权限失效则明确说明，不自行扩大权限。文档中的建议不代替运营商、主办方或店家的当前事实。

以下是**证据采信顺序**，不是机械的调用顺序：可以先用聚合工具找候选，再回第一优先级核实关键事实。同一事实有冲突时，核对日期、地区、门店、适用条件及来源更新时间；无法解释的冲突标待确认，不挑一个看似方便的结果。

| 研究内容 | 第一优先级：确认事实 | 第二优先级：查询、比较与交叉核对 | 第三优先级：体验与候选补充 |
| --- | --- | --- | --- |
| 日本交通 | 运营商当日适用时刻表、调图/停运公告、活动临时交通通知 | `yahoo_transit_readonly` MCP／本地 CLI 查询指定日期换乘；Yahoo、Jorudan、NAVITIME 的公开查询页面作交叉核对 | Google Maps 消费者导航、游客换乘经验；不能把经验用时写成精确班次 |
| 景点与活动 | 景点/主办方官网：营业、闭馆、门票、预约与活动日期 | `google-places-cli` 核对地点与评论样本；官方旅游局信息 | `xiaohongshu-travel-research` 通过已登录浏览器研究近期实访；旅游攻略仅作体验参考 |
| 餐厅 | Michelin 官方指南确认星级；餐厅官网与正式订位页面确认菜单、价格、营业及三人席位 | `michelin-guide-search` 找候选；`google-places-cli`、Tabelog 等分别核对评分、数量与评论日期 | 小红书与其他实访攻略补充服务、语言、口味信息，不代替星级或可订确认 |
| 住宿 | 酒店官网、目标房型条款及酒店明确回复确认三人入住、床型、前台和行李收寄；所选渠道最终房价页确认报价 | `hotel-stay-screening` 配合 `dida_readonly`／本地 CLI 查询同日期、人数、房型及退改条件；官方 OTA 页面交叉比价 | Google 与小红书近期评价补充噪音、卫生、位置和入住体验 |

- 规划与筛选使用 `trip-planner`、`japan-recs`；修改关西攻略、点位或地图时使用 `jap-tour-sync`。这些 skill 是工作流程，不是实时班次、库存或预约成功的证据。
- 交通必须显式查询旅行日期和日本时间，核对运营商、方向、换乘余量和完整返程。车站日型时刻表不等于某个未来日期已核实；“没有异常公告”不等于保证正常运行。首末班应检查能完成全部衔接的实用截止时间，并给出错过/停运备选。酒店未定时，其步行与接驳仍是估算。
- Google 仅通过已配置的本地密钥包装器调用；每次研究开始前确认仍为 Free Trial 且有余额/有效期，状态不明就暂停 API 调用并用公开网页。用户未授权产生实际付费、升级账单或购买套餐。预算提醒不是硬上限；Google Routes API 不能用于验证日本公交/铁路班次。评论最多五条样本，不代表全部评价。
- 酒店比较必须使用同一日期、三人入住方案、币种、税费、全程/每晚口径及退改条件。餐厅的订位链接不代表三人有位；酒店/餐厅候选不代表已经预订。研究不包含下单、锁房、付款或提交个人资料。
- **不得作为可用依据：** PriceWin 当前报价验证失败；小红书本机 MCP 有安全阻断，只用已授权的普通浏览器；NAVITIME 基础 API 的平均时间不是班次；尚未接入的商旅/付费 API 不能写成已具备。工具失效时记录原因并降级官网/网页，不编造结果、不绕过登录或安全限制。
- 关键结论记录来源链接、查询日期、旅行适用日期与“官方核实／聚合查询／经验参考／待确认”状态。不得将密钥、Cookie、完整评论或未经许可的照片/地图数据写入仓库；Google 内容的缓存、署名与展示遵守对应政策。本机工具不会随 Git 或网站发布自动安装到协作者电脑。

## 验证与交付

- 代码或内容配置变更运行 `pnpm run check`，补充相关回归；单版本检查是否误改另一版本，共享改动覆盖所有注册配置和受影响模板。
- 界面改动检查受影响的桌面、移动端和交互；涉及路由或资源时检查静态导出及部署前缀。未做浏览器验证要说明，不能用构建通过代替交互验证。
- 仅改文档时检查差异、链接和一致性即可。说明改了什么、验证了什么、还有什么待确认；不直接编辑构建产物，未经明确要求不发布或部署。
- 设计、研究、执行记录分别写入对应 `docs/`，在索引中链接；本文件不维护完整日程、目录清单或任务流水账。
