---
status: Active
owner: jap-tour
last_verified: 2026-09-05
sources:
  - docs/requirements/day-journals/weather-module.md
  - docs/development/journal-weather.md
  - tests/weather.test.mjs
  - tests/rendered-html.test.mjs
---

# 每日手账天气实现与验证

## 文档入口

- [仓库文档地图](../../MAP.md)
- [执行计划索引](index.md)
- [天气需求](../requirements/day-journals/weather-module.md)
- [开发设计](../development/journal-weather.md)

## 本轮完成：2026-09-05

从既有日期、坐标与路线派生天气上下文，为所有已注册手账接入当地当前天气、当日预报、沿途分时、选点、刷新与异常展示。使用共享领域模型和组件，原两套配置的行程内容未改变；两种手账模板均覆盖。

今天展示最新当地当前天气，未来只展示手账日期预报。当前实际日期为 2026-09-05，关西九月底的手账暂不在供应商预报范围内，页面应如实显示“暂不可用”和预计查询日期。

## 自动化验证

- `pnpm run check` 通过：TypeScript、全仓 ESLint、9 项天气回归、构建和 16 项服务端渲染测试。
- 天气回归覆盖所有注册配置的不可变派生、跨区域坐标、保留返店、目的地时区午夜、预报边界、当前数据有效时间、空值与零值、响应日期不符、多地点部分失败、HTTP/网络错误及提示阈值。
- 页面回归遍历全部注册手账，验证日期、模板、路线之后的插入位置、来源与默认收起状态。
- 在 `GITHUB_ACTIONS=true`、`GITHUB_REPOSITORY=test/play-20261001-jap-tour` 下再次运行 `pnpm run check`，20 个静态页面成功导出，0 跳过，同样通过全部 25 项测试。天气卡、返回路径、脚本和样式带站点前缀；仅保留既有 Leaflet CSS 图标路径构建提示。
- 本机 pnpm 默认依赖核验尝试重装现有依赖；以进程变量 `pnpm_config_verify_deps_before_run=false` 使用现有安装完成检查，无依赖或锁文件变更。

## 浏览器验证

验证路径：打开手账 → 切换当天地点 / 展开分时 / 刷新 → 对应天气与状态更新，正文和返回链接可用。

Browser plugin not available；使用运行时已附带的 Playwright 1.62.1 与本机 Chrome（headless），未安装依赖。目标 `http://localhost:3000`，桌面 1440 × 1000、手机 390 × 844。遵循仓库界面改动验证要求；脚本、截图留在仓库外。

| 检查 | 结果 |
| --- | --- |
| 页面身份与正文 | 配置 1、配置 2、开发示例的日期、标题、返回链接正确；页面非空 |
| 框架错误 | 无错误浮层、无页面脚本异常 |
| 桌面 / 手机布局 | 两种模板已截图检查；无横向溢出、遮挡或控件不可用 |
| 交互 | 地点选择更新温度，鼠标与键盘均能展开，刷新保留地点与展开状态 |
| 异常 | 未来不可用、已过去、过期、部分地点缺失、断网后保留上次结果均通过 |
| 配置切换 | 配置 2 跨区域日请求自己的日期和点位；示例无关西内容，状态重置 |
| 静态导出交互 | 在本地按部署前缀提供原始导出文件，三套配置手账和 `/day-1.html`、`/day-2.html`、`/day-3.html` 共六个入口均加载客户端资源、展开与刷新正常，手机无溢出 |
| 控制台 | 无未解释错误；既有 `/favicon.ico` 返回 404，断网用例产生预期 `ERR_FAILED` |

日期内天气、过期和断网状态使用浏览器时钟及明确的接口测试数据验证，不作为真实预报证据。另从浏览器直接请求真实 Open-Meteo 接口，HTTP 200，跨域可读，关西机场与大阪两个坐标分别返回当前天气和各 24 个小时数据；日期 2026-09-05、有效时刻 16:30（Asia/Tokyo）。

## 后续范围

### 界面补充：2026-09-05

按用户要求移除共享天气卡底部来源链接与整段预警／停运说明，两种模板生效；需求和开发文档同步。`pnpm run check` 再次通过全部 25 项测试。

截图仅在临时浏览器响应中把天气日期替换为 2026-09-05，未改行程配置。使用构建后的本地页面 `http://localhost:3001` 请求真实接口（3 次均 HTTP 200），完成 1440 × 1000 和 390 × 844 截图；展开、刷新、模板显示正常，无页面错误或手机横向溢出。开发页面临时替换曾触发 Vite 热更新错误，改用生产构建预览后通过。正式页面日期仍为原行程日期。

官方预警、场所停运公告自动汇总及严重状态验收仍未完成。单位切换、历史天气和覆盖完整停留区间的风险汇总留待后续。此轮完成实时天气基础交付，不将完整天气需求的全部验收项标为已完成。

2026-09-05 用户确认提交并推送。截图与验证使用的三个仓库外临时脚本已删除；正式源码无临时日期覆盖，截图文件不进入提交。推送与自动部署结果以对应 Git 提交及 CI 记录为准。
